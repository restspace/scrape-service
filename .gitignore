node_modules/

# Runtime state — job records and captured artefacts. Never committed; the
# server writes these into ARTEFACT_ROOT (in production, RS2's fileRoot).
/var/
/artefacts/
*.log

# Parity baselines are full captures of third-party business websites, and the
# site list points at real businesses. Neither belongs in a public repository —
# generate them locally with `npm run parity:record`. See
# test/parity-sites.example.json.
test/fixtures/
test/parity-sites.json

.DS_Store
Thumbs.db
